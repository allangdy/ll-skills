# PLAN-REVIEW — phase 05 — 2026-09-10
mode: plan · one pass · plan: phases/05/PLAN.md · contract: ROADMAP.md "Phase 05" SC-01..SC-03 · PLAN.md §2/§6/§7
verdict: REJECTED
lint: plan-lint pass (0 error, 0 warn) — mechanical defects not repeated here

| # | question | answer | milestone or line | severity |
|---|---|---|---|---|
| 1 | every milestone has an executable `acceptance:` | no — M1/M2/M4 yes; **M3's chain can exit 0 with the smoke section red and the rep dead**. When `bash scripts/smoke-test.sh --only evals-auto \| tail -1 \| grep -q 'smoke test OK — 4 checks'` fails, the `&&` list short-circuits and `test "${PIPESTATUS[0]}" -eq 0` reads the first stage of that failed pipeline, not the rep (reproduced in this session: `printf x \| tail -1 \| grep -q NOPE && …; PIPESTATUS0=0`), and the closing `grep -q '^results : ' /tmp/ll-evals-real.txt` then matches a **stale** file from an earlier attempt. `results : <path>` is printed at `scripts/evals/run.sh:113`, before any case runs, so that grep is also green for a rep that failed every case. Second hole: SC-02 is "one real rep passes, **with the `RESULTS.md` path recorded in PROGRESS.md**" — no milestone has an executable check of the PROGRESS.md half (D-05-04 leaves it to the return block). Note in favour: the `'— 4 checks'` count guard is correct (a missing section prints `OK — 0 checks` and exits 0; verified with `--only goal-autonomo`) | PLAN.md:34 (M3 acceptance), PLAN.md:81 | major |
| 2 | every `truth:` in §1 has a milestone naming it | partial — the two truths this phase claims are named (T3 by M1/M2/M3, T6 by M4), but **the string T3 is proved by is asserted nowhere**. T3 (PLAN.md §1:25) is `… detect --json → JSON with one status per stage, `decide: done``, and CA-04 (PLAN.md §6) says the `auto-dry-run` eval SHALL show "the stage table with `decide: done`". M1's assert (D-05-02) checks a `phase-07` row, a `phase-08` row and a `close` row only, and `pass.txt` is defined as exactly those three lines plus one English sentence. Verified in this session: `ll-auto.js roteiro --cwd scripts/fixtures/project` prints `1. phase-07 … 2. phase-08 … 3. close` and **no** `decide: done`; `decide: done` comes from `detect`, a different table. The phase would ship CA-04's named sentinel without CA-04's assertion | PLAN.md:60 (M1 action), phases/05/DECISIONS.md D-05-02 | major |
| 3 | every `DEC-` cited appears in a milestone or in the freedoms | partial — D-05-01…D-05-06 all land (M1: 01/02/03 · M2: 01/02 · M3: 03/04 · M4: 05/06). `DEC-0003` (no spending ceiling) is cited in DECISIONS §Score and appears in no milestone and no freedom; the one place phase 05 writes about cost is M4's README line "≤ 6 turns, cents", tied to no decision and to no measurement | PLAN.md:91 (M4), phases/05/DECISIONS.md §Score | minor |
| 4 | scope reduction without a deferred item | yes, one — D-05-03 **amended** replaces `fail.txt` (an answer the assert rejects) with `/dev/null`, no deferred item. It is not cosmetic: `contains()` opens with `[ -f "$1" ]` (`scripts/evals/lib/assert.sh:31`) and `/dev/null` is a character device, so **every `contains` fails on the file-type guard whatever the regex is**. The "rejects the empty answer" half of M1, M2 and of all 4 smoke checks in M3 is therefore green for an assert whose roteiro regexes are wrong, inverted or vacuous — the exact failure the offline proof exists to catch. (The bigger cut — implement-to-close on a real fixture — *is* properly deferred: ROADMAP "Deferred ideas" + DECISIONS §Deferred) | PLAN.md:12, :23, :34, :60, :80; DECISIONS.md last line | major |
| 5 | `depends_on:` and `files:` agree | yes — M3 reads/edits M1+M2 files and declares `depends_on: [M1, M2]`; M4 `depends_on: [M3]`; M1 and M2 are file-disjoint in wave 1. Every cited `read_first` range re-resolved in this session: `smoke-test.sh:331` VERSION check ✓, `:497-505` lint-orquestrador ✓, `:549-565` goal-autonomo ✓, `ll-auto/SKILL.md:36-42` flow step 0 with the two empty-repo lines at :39 ✓, `run.sh:157-166` prepare_workdir ✓; and `run.sh:225-247` does print `cp -R <case>/fixture/. …`, so M2's `grep -q 'cases/auto-empty-repo/fixture'` is a real check of the own-fixture wiring | — | none |
| 6 | a milestone over 5 files or over 3 tasks | files no (max 5: M2, M3). Tasks yes — **M3 carries four**: the offline smoke section, the paid `claude -p` rep, a conditional `prompt.txt` rewrite, a conditional `max_turns` bump. It also declares `verification: internal` while `exclusive: [claude-cli]` and DECISIONS §Accepted risks admit the rep depends on an external CLI, the owner's credentials and model behaviour: an external criterion tagged internal | PLAN.md:29-39, :80 | medium |
| 7 | a numeric value without a source | mostly sourced (6/4 turns, `min_pass` 1 → D-05-01 · `--reps 1` → D-05-04 · 4 checks → D-05-03 amended · 3.0.0, 2026-09-10, "12 skills" → D-05-05 · twelve cases → D-05-06). Two without: M3's "raise `max_turns` **by 2**" (D-05-01 §Revisable authorises "raise once, record the number", not the step) and M4's README "**cents**", a cost figure written by the milestone that runs after the only rep that could measure it | PLAN.md:80, :91 | minor |
| 8 | a rule in §2 without a source | one — M1's `absent "$OUT_TXT" '▶ Next — /clear, then ll-(implement\|close\|research\|brainstorm\|decide)'`. `skills/ll-auto/SKILL.md:38-40` says `--dry-run` prints the roteiro and stops before writing `docs/AUTO.md`; it nowhere prohibits a handover line, and the skill's own closing line is `▶ Next — /clear, then ll-resume` (SKILL.md:69). D-05-02's backing cites D-03-06 and D-03-08 (both exist; D-03-08 governs `--pause-at`, not `--dry-run`). Coupled with M3's "never change an assert", a real rep whose answer ends `▶ Next — /clear, then ll-implement 07 --no-talk` fails with no authorised repair branch (only prompt rewrite and turn bump are allowed) | PLAN.md:60 (M1), PLAN.md:80 (M3) | medium |

BLOCKS: none — no milestone block names a commit or an existing-file range that git does not have; every `read_first` line reference resolved.

Disconfirmation: 1 partial requirement — SC-02's "with the `RESULTS.md` path recorded in PROGRESS.md" has no executable check in any milestone (only D-05-04 prose). 1 test that passes without testing — the `/dev/null` rejection in M1/M2/M3 fails on `contains`'s `[ -f ]` guard (`lib/assert.sh:31`), never on the regex, so it scores an assert that discriminates nothing as green. 1 uncovered error path — M3 enumerates repairs for `error_max_turns` and for an unexpanded slash command, but not for the likeliest FAIL of a first rep (an assert regex that does not match a truthful answer), and forbids touching the assert.

What this review does NOT prove: that the two cases pass a real `claude -p` rep; that `claude -p` expands `/ll-auto` in print mode; that a `12 skills` description and `## [3.0.0] - 2026-09-10` keep `npm run lint && npm test` green.

Gaps: G-1 M3 acceptance is false-green · amend M3 · acceptance: `set -o pipefail; rm -f /tmp/ll-evals-real.txt; bash scripts/smoke-test.sh --only evals-auto 2>&1 | tail -1 | grep -q 'smoke test OK — 4 checks' && bash scripts/evals/run.sh --dry-run --case auto-dry-run --case auto-empty-repo >/dev/null && bash scripts/evals/run.sh --case auto-dry-run --case auto-empty-repo --reps 1 2>&1 | tee /tmp/ll-evals-real.txt | grep -Eq '^\| *auto-dry-run *\| .*PASS'` exit 0
Gaps: G-2 the negative offline check proves nothing · amend M1, M2 and M3 (D-05-03) · acceptance: `bash scripts/evals/cases/<case>/assert.sh "$W" "$W/out.json" scripts/fixtures/evals-auto/<case>/fail.txt` exit 1 where `fail.txt` is a **regular file** holding a plausible-but-wrong answer (roteiro rows for the wrong phases / the empty-repo lines with `docs/AUTO.md` announced as written), and `grep -c FAIL: <assert.log>` ≥ 1
Gaps: G-3 CA-04 / T3's `decide: done` is asserted nowhere · amend M1 (assert + `pass.txt`) · acceptance: `grep -q 'decide' scripts/evals/cases/auto-dry-run/assert.sh && grep -q 'decide: done' scripts/fixtures/evals-auto/auto-dry-run/pass.txt && bash scripts/evals/cases/auto-dry-run/assert.sh "$W" "$W/out.json" scripts/fixtures/evals-auto/auto-dry-run/pass.txt` exit 0
Gaps: G-4 SC-02's PROGRESS.md half is unchecked · amend M3 · acceptance: `R=$(grep -m1 '^results : ' /tmp/ll-evals-real.txt | sed 's/^results : //'); grep -q "$R/RESULTS.md" PROGRESS.md` exit 0
