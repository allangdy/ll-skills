# VERIFICATION — phase 03 — 2026-09-10
mode: plan · slice: main (working tree, `phases/03/` untracked)
verdict: REJECTED

| # | question | answer | milestone or line | severity |
|---|---|---|---|---|
| 1 | Does every milestone have an executable `acceptance:`? | Yes — four commands, all with an exit code, all runnable from the repo root. But two ROADMAP criteria have no acceptance that could fail: SC-03 (`docs/AUTO.md` = objective, flags, roteiro, status per stage) is proven by a single `grep -q '^## Roteiro'`, one of the five sections D-03-07 mandates; SC-04's "and only it" half is proven by nothing — `npm run lint` green shows the rule-1 map accepts ll-auto's `allowed-tools`, never that it rejects the same value in another skill. Rule 5's `/ll-` and ``run `ll-x` `` checks already skip every non-`SKILL.md` file (lint-prompts.sh:271), so the planned `ORCHESTRATOR` exception "for references" guards a path the lint never walks, and its absence would not turn M3's acceptance red. | M2 acceptance (PLAN.md:23); M3 acceptance (PLAN.md:34); lint-prompts.sh:259-282 | high |
| 2 | Does every `truth:` in PLAN §1 have a milestone naming it? | Yes for the two truths this phase owns: T3 → M1, M2; T6 → M3, M4. T1/T2/T4/T5 belong to phases 01, 02 and 04 and are correctly absent. | PLAN.md:14,25,36,47 | none |
| 3 | Does every `DEC-` cited in PLAN or DECISIONS appear in a milestone or in the freedoms? | Almost. D-03-02..D-03-08 are each named in a milestone body. **D-03-01 is named nowhere in the phase plan** — the decision that fixes the helper path, the "no dependencies / never a copy of ll-tools.js" rule (I-08) and the five-command surface is carried only as paraphrase in M1/M2 prose. DEC-0001/0002/0005 arrive through D-03-03/D-03-05, acceptable. | grep of `D-03-0[0-9]` over PLAN.md returns 02,03,04,05,06,07,08 — never 01 | low |
| 4 | Scope reduction in the wording without a deferred item? | No occurrence of "v1", "for now", "later", "simplified". The two reductions are recorded: `--interactive` beyond brainstorm/phase and the eval cases `auto-dry-run`/`auto-empty-repo` sit in DECISIONS §Deferred with phase 05 as target. Consequence to keep visible: R-03's and CA-04/CA-05's sentinels are those evals, so this phase ships R-03 as prose in SKILL.md with no executable proof. | DECISIONS.md §Deferred; PLAN.md §4 R-03, §6 CA-04/CA-05 | low |
| 5 | Do `depends_on:` and `files:` agree — does a milestone read a file a later milestone creates? | Ordering is sound: M2 reads the helper M1 creates and declares `depends_on: [M1]`; M3 and M4 read only files present at HEAD. Two `read_first` citations point at the wrong lines: `ll-tools.js:418-446` is `C.state` (417-427), `C.heartbeat` (429-443) and the head of `C.ledger` — the `--json`/human dual output and the `{"ok":false,"reason":…}` shape live at :670-678; `ll-tools.js:434` is heartbeat's insertion point, while the `## Epilogue — phase NN` parser is at :545. M4's `smoke-test.sh:230-266` and M1's `:109-120`, `:237` check out. Also: the smoke bump at :237 is a bare count — the explicit per-skill loop at :238-240 does not list `ll-auto`. | PLAN.md:56,58 vs scripts/ll-tools.js:417-443,545,670-678 | medium |
| 6 | Does any milestone touch more than 5 files or more than 3 tasks? | Files: 2/4/4/4, all under the ceiling. Tasks: M2 ships four new helper commands plus a two-file fixture (5 tasks); M4 ships installer + smoke + README + CHANGELOG (4). Worse, **M3 is `tdd: no` while it changes lint behaviour** — PLAN §7 reads "TDD on for behaviour (the `ll-auto.js` helper, lint rules with fixtures); off for prompt text", and M3 rewrites rule 1's `allowed-tools` check into a per-skill map and adds an `ORCHESTRATOR` list to rule 5 with no fixture and no red-first commit. | M2 (PLAN.md:18-28), M4 (PLAN.md:40-50), M3 (PLAN.md:33) vs PLAN.md §7 | high |
| 7 | Is there a numeric value without a source? | One: `≤ 400 lines for the whole phase` for `ll-auto.js` — no citation, and lint-prompts rule 3 sets ceilings only for `skills/*/SKILL.md` (200), agents (160), `references/*.md` (150), `assets/preamble.md` (70) and `scripts/ll-tools.js` (700/32768); nothing caps `skills/*/scripts/*.js`, so the number is neither enforced nor sourced. Minor: the README flag table "(10 lines)". Sourced and checked: 200 and 60–300 chars (rule 3 / ASM-1), `0o755` (bin/install.js:203-211), Node ≥ 18 (§9), 11 → 12 installed skills (smoke-test.sh:237 = 11 today; bin/install.js `planFiles` copies any `skills/ll-*` tree with or without a SKILL.md, so the bump lands at M1). | PLAN.md:60, PLAN.md:95 | low |
| 8 | Is there a rule without a source? | Two under-sourced rules. (a) `next-cmd` is specified against the grammar `▶ Next — /clear, then <cmd>` with "parenthetical stripped, trimmed", but the fixture line it must parse is ``▶ Next — `/clear`, then `ll-implement 8` `` (fixtures/project/PROGRESS.md:61) — backtick stripping is required by the acceptance and stated nowhere. (b) M2's "`half` ones first as resume" ordering appears in no decision (D-03-04 only says a half phase resumes with the same command) and in no GSD line cited. Correctly sourced and re-checked against the files: the amended D-03-02 phase rule (the fixture's `ll-state` block is phase 07 with M2/M3 `passes: false` → `half` ✓), `verify-NN` todo (no `phases/07/VERIFICATION.md` in the fixture, only a root-level one ✓), and the claim that rule 7 tolerates the Portuguese D-03-06 sentence inside backticks (lint-prompts.sh:328 skips any line containing a backtick ✓). | PLAN.md:72; scripts/fixtures/project/PROGRESS.md:61 | medium |

BLOCKS: none (plan mode — no commits, no tests to run yet)
Disconfirmation: 1 partial requirement — SC-04 is delivered half (the lint *allows* ll-auto; nothing in the plan makes the lint *reject* the same `allowed-tools` or `/ll-` line in another skill). 1 acceptance that passes without testing — M4's `grep -q 'll-auto' README.md` is already true at HEAD (README.md:40, "ll-auto (fase 03, em desenvolvimento)"): it passes before M4 writes a single line; only the CHANGELOG grep is meaningful today. 1 uncovered error path — `detect --cwd <not a directory>` is named in M1's behavior line (`{"ok":false,"reason":…}`, exit 0) but appears in no acceptance and in no planned smoke check; the sibling trap, `--cwd` pointing inside this git repository while the helper copies `gitTop` (M1 read_first), is not resolved anywhere: nothing says whether `--cwd` or the git top wins, and the whole phase's fixtures live inside this repository.
What this review does NOT prove: that the helper can be written in 400 lines; that the roteiro semantics match `~/.claude/gsd-core/workflows/autonomous.md` (file not read in this session); that `npm run lint` and `npm test` are green at HEAD (not run — this is a plan review); that the intermediate tree after M1/M2 (a `skills/ll-auto/` with no SKILL.md) keeps lint and smoke green, since neither M1 nor M2 runs them.
Gaps: G-1 SC-04 "and only it" — amend **M3**: add a lint fixture and a `tdd: yes` red-first commit · acceptance: `d=$(mktemp -d) && git archive HEAD | tar -x -C "$d" && cd "$d" && git init -q . && sed -i 's|^allowed-tools:.*|allowed-tools: Bash(${CLAUDE_SKILL_DIR}/scripts/ll-auto.js *)|' skills/ll-resume/SKILL.md && git add -A && ! bash scripts/lint-prompts.sh .` exit 0 (the tree must be a git repo: lint-prompts enumerates via `git ls-files`)
Gaps: G-2 SC-03 `docs/AUTO.md` body — amend **M2** acceptance: `[ "$(node skills/ll-auto/scripts/ll-auto.js auto-md --cwd scripts/fixtures/auto-decisions --objective 'x' --flags '--verify all' | grep -c '^## \(Objective\|Flags\|Roteiro\|Decisions taken alone\|Log\)')" = 5 ]` exit 0, plus one assertion that the roteiro table carries a `status` column value per stage
Gaps: G-3 M3 `tdd: no` contradicts PLAN §7 ("lint rules with fixtures") — amend **M3** to `tdd: yes` (or record a DEC amending §7) · acceptance: `git log --format='%s' <range> | grep -n 'test(M3)' && git log --format='%s' <range> | grep -n 'feat(M3)'` with `test(M3)` first
Gaps: G-4 M4's README grep is vacuous at HEAD, D-03-01 is cited by no milestone, and M1's `read_first` cites `ll-tools.js:418-446`/`:434` for code that lives at `:670-678`/`:545` — amend **M4** acceptance to `grep -q 'Fluxo autônomo' README.md && grep -q 'll-auto' CHANGELOG.md` and fix the two citations in **M1**
