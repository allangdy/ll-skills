# Phase 03 — Decisions · 2026-09-10 · ll-implement

## Score
questions asked 0 / assumptions 8 / band-1 open 0. `--no-talk`: A ratified whole; no B item (nothing leaves the repo, no money, no customer data, no scope cut — the flags and their meaning were fixed by the owner on the design page and in DEC-0001..0006).

## Locked
- D-03-01 — the helper is `skills/ll-auto/scripts/ll-auto.js`: Node ≥ 18, no dependencies, shipped inside the skill folder, never a copy of `ll-tools.js` (I-08: the shared helper is at its ceiling). Commands: `detect [--cwd <dir>] [--json]` (stage table), `roteiro --flags "<args>" [--cwd] [--json]` (the ordered list of stages to run, from `detect` plus the flags), `next-cmd <file>` (the command of the last `▶ Next — /clear, then <cmd>` line in a file, parenthetical stripped), `report [--cwd] [--json]` (every `decisions/*.md` carrying `[decided by absence — revisable]`), `auto-md` (renders `docs/AUTO.md` from objective, flags, roteiro and the current detect) · class RULE · band 2 · impact HIGH · revert 1 phase · backing: PLAN §2 I-08; ROADMAP SC-01/SC-03; design page "Começar do meio" · decided by: Claude.
- D-03-02 — stage detection rules, deterministic (R-02): `research` done when a `docs/research-*/SUMMARY.md` exists · `brainstorm` done when `docs/decide/OPENING.md` or any `phases/NN/DECISIONS.md` exists · `decide` done when `PLAN.md` exists at the root together with `PROGRESS.md` carrying an `ll-state` block (ROADMAP is optional: it exists only above 3 phases) · `phase NN` for every row of the ROADMAP table (or PLAN §8 inline when there is no ROADMAP): `done` when the row state starts with `DONE` or `PROGRESS.md` carries `## Epilogue — phase NN`; `half` when `phases/NN/PLAN.md` exists and there is no epilogue for NN; else `todo` · `verify NN` done when `phases/NN/VERIFICATION.md` exists; it enters the roteiro only when the NN epilogue names `ll-verify NN` or `--verify all` is set · `close` done when `docs/DELIVERY.md` exists and PROGRESS.md carries an epilogue for the last ROADMAP phase. On `scripts/fixtures/project`: research todo, brainstorm todo, decide done, 05 done, 06 done, 07 half, 08 todo, close todo · class RULE · band 2 · impact HIGH · revert 1 commit · backing: design page table "Começar do meio"; `scripts/fixtures/project/ROADMAP.md`, `PROGRESS.md` · decided by: Claude.
- D-03-03 — `ll-auto` follows a stage by reading the stage skill's `SKILL.md` from `${CLAUDE_CONFIG_DIR:-$HOME/.claude}/skills/<skill>/SKILL.md` (falling back to the package `skills/` tree when run from the repository) with the `Read` tool and executing its Flow in place with the stage's arguments; it never calls the Skill tool (lint-prompts rule 5 keeps forbidding `Skill(` everywhere, `ll-auto` included) · class RULE · band 2 · impact HIGH · revert 1 phase · backing: DEC-0002, DEC-0005, `assets/preamble.md` "ll-auto is the single place that follows another skill's instructions" · decided by: owner (DEC-0005) · consequences: lint-prompts rule 5's ban on "run `ll-x`" and on lines starting with `/ll-` gets one exception list `ORCHESTRATOR = ["ll-auto"]`; rule 1's `allowed-tools` accepts `Bash(${CLAUDE_SKILL_DIR}/scripts/ll-auto.js *)` for `ll-auto` only.
- D-03-04 — stage arguments: research → `ll-research "<objective>"`; brainstorm → `ll-brainstorm project --no-talk` (`--interactive` drops `--no-talk`); decide → `ll-decide project --no-talk`; phase NN → `ll-implement NN --no-talk` (`--interactive` drops `--no-talk`; a `half` phase resumes with the same command, `ll-implement` reads the board); verify NN → `ll-verify NN` (never `--external` inside ll-auto: the flag hands the audit to another session, which ll-auto is not); close → `ll-close --no-talk` (`--milestone <name>` when the ROADMAP is complete) · class RULE · band 2 · impact MED · revert 1 commit · backing: phase 02 D-02-03/D-02-04; `skills/ll-verify/SKILL.md:33` (`--external` stops in the implementing session) · decided by: Claude.
- D-03-05 — decisions only the owner can take: after every stage `ll-auto` lists `decisions/*.md` in state `WAITING`. Without `--auto-decision`: the run continues through stages that do not depend on them (a phase whose ROADMAP section carries `stop: owner` is skipped) and stops when nothing else can run, printing the WAITING list and `▶ Next — /clear, then ll-auto --resume`. With `--auto-decision`: each WAITING DEC is resolved to its recommended option by appending a dated `status: DECIDED — <option> [decided by absence — revisable]` line, `stop: owner` marks are cleared in ROADMAP, and the run continues; the end-of-run block lists every file carrying the marker (`report`) · class RULE · band 2 · impact HIGH · revert 1 commit · backing: DEC-0001 (owner, verbatim); phase 02 D-02-03 · decided by: owner (DEC-0001).
- D-03-06 — empty repository (no research, no OPENING, no PLAN) without an objective: print `Nada encontrado neste repositório: sem pesquisa, OPENING.md nem PLAN.md.` and the command to complete, `/ll-auto "<objetivo>" [--research] [--brainstorm]`, then stop — no AskUserQuestion (R-03). With an objective: the roteiro starts at research (if `--research`), brainstorm (if `--brainstorm`) or decide · class RULE · band 2 · impact MED · backing: owner, 2026-09-10 ("se não tiver, ele vai falar que não encontrou nada") and R-03 · decided by: owner.
- D-03-07 — `docs/AUTO.md` is the run's state file: frontmatter-free Markdown with `## Objective`, `## Flags`, `## Roteiro` (table `| # | stage | command | status | evidence |`, status ∈ todo · half · running · done · skipped · waiting), `## Decisions taken alone` (filled at the end), `## Log` (one dated line per stage transition). `--resume` re-reads its `## Flags`; a plain `/ll-auto` in a repo with `docs/AUTO.md` also continues from disk (the flags are the file's) · class RULE · band 2 · impact MED · backing: design page "O que fica em disco" · decided by: Claude.
- D-03-08 — `--pause-at <stage|N>` stops after that stage or phase with `▶ Next — /clear, then ll-auto --resume`; `--from/--to/--only` filter the phase rows as GSD does (`--only N` skips close); `--redo <stage>` forces a `done` stage back to `todo` in the roteiro; `--dry-run` prints the roteiro table and stops before writing `docs/AUTO.md` · class RULE · band 2 · impact LOW · backing: design page "Flags"; `~/.claude/gsd-core/workflows/autonomous.md` (--from/--to/--only semantics) · decided by: Claude.

## Implementer freedoms
Internal layout of `ll-auto.js`; JSON field names beyond `stages[].id/status/evidence` and `roteiro[].stage/command/status`; wording of `docs/AUTO.md` prose; how the smoke test builds its scratch trees.

## Revisable
- D-03-02 "phase done" rule (ROADMAP `DONE` or epilogue present). Review trigger: a phase with an epilogue whose `left:` is not empty is treated as done — if that bites in phase 05, add "and `left: none`".
- D-03-04 `ll-verify NN` without `--external`. Review trigger: the owner wants an independent audit inside autonomous runs (then ll-auto would need a headless session, rejected in DEC-0005).

## Deferred
- `--interactive` beyond brainstorm and the phase conversation (ROADMAP deferred idea).
- Eval cases `auto-dry-run` and `auto-empty-repo` — phase 05.

## Against the recommendation
none

## Owner's free answers (verbatim)
- "Se eu rodar o comando de alto, sem passar nada, e ele vai ver que tem algo pela metade… se não tiver, ele vai falar para mim que não encontrou nada" (2026-09-10) — carried as D-03-06 with the correction the session gave: it prints the command to complete, it does not ask.

## Accepted risks
- `ll-auto` runs the whole cycle in one context (DEC-0005); a long run compacts. Every stage writes its output to disk before the next starts, and `docs/AUTO.md` plus `--resume` recover the position.
- D-03-02 (amended 2026-09-10, before the plan review): a phase with an epilogue is `done` only when the `ll-state` block is for another phase or shows every milestone `passes: true`; an epilogue over a red board (the fixture's phase 07) is `half`.
