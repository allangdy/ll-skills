# PLAN-REVIEW — phase 04 — 2026-09-10
mode: plan · file: phases/04/PLAN.md · contract: ROADMAP.md "Phase 04" (SC-01..SC-03) · lint: pass (not repeated)
verdict: REJECTED

| # | question | answer | milestone or line | severity |
|---|---|---|---|---|
| 1 | Does every milestone have an executable `acceptance:`? | Both are commands with exit codes, but two of M1's eight conjuncts pass by absence — verified in this session: `bash scripts/smoke-test.sh --only goal-autonomo` on a section that does not exist prints `smoke test OK — 0 checks` and exits 0; the awk extractor yields 0 bytes (≤ 4000 → true) when the `## Autonomous example` heading is missing, when no fence follows it, or when the fence is opened as ```` ```text ```` instead of a bare ```` ``` ````. An M1 that ships the SKILL.md flag and nothing else passes those two links | PLAN.md:12 (acceptance), :38 (smoke section), :39 (`behavior`, which claims a red for "missing example" that this check can never produce) | error |
| 2 | Does every `truth:` in PLAN §1 have a milestone naming it? | Yes — T5 → M1, T6 → M2. But T6 / SC-03 (`npm run lint && npm test` exit 0) is red at HEAD: smoke-test.sh:213 ("hook ignora PROGRESS.md dentro de fixtures/") passes only with the owner's uncommitted `hooks/ll-state.js` (`'fixtures'` in the skip set). Reproduced here with `git show HEAD:hooks/ll-state.js` → non-empty output = check fails; with the worktree file → empty = passes. M2's acceptance therefore certifies the dirty worktree, not the slice, and PLAN §2 I-09 forbids the executor from committing those two files. No milestone, `stop:` or deferral in the phase plan records this | PLAN.md:23 (M2 acceptance), :50; PLAN.md I-09 + Errata; scripts/smoke-test.sh:213 | error |
| 3 | Does every cited `DEC-` appear in a milestone or in the freedoms? | D-04-01…D-04-05 all appear in M1's `action` (M2 cites none, docs-only — acceptable). DEC-0001/0003/0004 are carried through D-04-01/D-04-03. Missing: M1 edits and commits `scripts/smoke-test.sh`, which PLAN §2 I-09 protects; only the PLAN `## Errata` line and DEC-0009 lift that, and neither is cited by the phase plan | PLAN.md:9, :38; PLAN.md §2 I-09 / Errata; decisions/DEC-0009-smoke-test-editable.md | warn |
| 4 | Scope reduction in the wording without a deferred item? | No "v1 / for now / later / simplified" anywhere (grep clean). One deferral exists — the eval case that renders and measures the autonomous text — but it lives only in DECISIONS `## Deferred`; it is not in the plan's `## Errata` and has no `BACKLOG.md` row, so it will not survive the phase | phases/04/DECISIONS.md `## Deferred`; PLAN.md:53 (`## Errata`, empty); BACKLOG.md | warn |
| 5 | Do `depends_on:` and `files:` agree? | Yes. M1 creates the `goal-autonomo` section; M2 `depends_on: [M1]` and consumes it through `npm test`. No milestone reads a file a later one creates. Cited ranges resolve: smoke-test.sh:12-19 (`--only`/`section()`) ✓, :281-289 (4d grep-on-generated-text) ✓, :497-505 (`lint-orquestrador`, `if section` at :500) ✓, lint-prompts.sh:264-291 (rule 5) ✓, README.md:53/:80 (both `ll-goal` rows) ✓; only "Fluxo autônomo" drifted (heading at README.md:90, plan says 92-110) | PLAN.md:10, :21, :33-37, :46 | info |
| 6 | Any milestone over 5 files or over 3 tasks? | Files: 3 (under the limit). Tasks: over — M1 carries (a) variant frontmatter, (b) eight parts of the text, (c) a separate worked example, (d) three checklist lines, plus SKILL.md argument-hint + description + a `## Autonomous mode` section + step-2/3/4 rewrites, plus a new smoke section with four checks and its header registration: ~9 sub-edits across 3 artifacts in one `test`/`feat` pair. The `action:` paragraph is ~900 words, the whole of the phase's behaviour | PLAN.md:7-17, :38 | warn |
| 7 | A numeric value without a source? | Sourced: 4,000 ← ROADMAP SC-02 / D-04-05; 150 and 200 ← lint-prompts.sh:184-185/:182; "five fields" ← goal-template.md:90 checklist item 10; `max_turns` stays the `<n>` placeholder. Unreconciled: the file has 60 lines of headroom (90/150) and a 4,000-char example alone costs ~45 of them at the template's ~90-char lines, before the frontmatter block, the parts and the 3 checklist lines. The plan asserts both ceilings and never states the line budget that makes them compatible | PLAN.md:12 (`-le 4000`, `-le 150`), :38 ("Whole file ≤ 150 lines") | warn |
| 8 | A rule without a source? | "never `/clear` between stages", written into the emitted text as an absolute, has no source and runs against the orchestrator's own shipped stop shape (`▶ Next — /clear, then ll-auto --resume` at skills/ll-auto/SKILL.md:56, references/run.md:51, references/stages.md:55). The other rules are sourced: DONE WHEN ← D-04-05, band-1 stop list ← PLAN §7 + goal-template.md:49-51, models per role ← PLAN §7 | PLAN.md:38 ("never `/clear` between stages") | warn |

BLOCKS: none (plan mode — the defects above are the return)

Disconfirmation: 1 partial requirement — SC-02 asks for the mode "documented in SKILL.md **and** argument-hint", and M1's acceptance greps `--autonomous` in the whole file plus in the `argument-hint:` line; a SKILL.md whose only change is the hint satisfies both conjuncts, so the `## Autonomous mode` prose the plan promises is never checked. 1 check that passes without testing — the `awk … | wc -c` ≤ 4000 conjunct (0 bytes on a missing heading, a missing fence, or a tagged fence: all four cases run here). 1 uncovered error path — nothing in M1's acceptance asserts that the `## Autonomous variant` section, its five-field frontmatter, the `mode: autonomous` line or the three extra checklist lines exist; `npm run lint` only counts lines and FORBIDDEN words, so item (a) and item (d) of the milestone can be silently dropped and the milestone still exits 0.

What this review does NOT prove: that the M1/M2 acceptances go green once written (no code exists yet); that `npm test` passes anywhere (not run — the phase-04 conclusion about it is from the single check at smoke-test.sh:213, reproduced in isolation against `git show HEAD:hooks/ll-state.js`); that the emitted `/goal` text is ≤ 4,000 chars in a real session (the plan measures a static example, an accepted risk in DECISIONS).

Gaps: G-1 M1 acceptance passes on absence · amend M1 — add non-vacuity to the chain: `grep -q '^## Autonomous variant' skills/ll-goal/references/goal-template.md && grep -q '^## Autonomous example' skills/ll-goal/references/goal-template.md && [ "$(awk '/^## Autonomous example$/{f=1;next} f&&/^```/{if(b){exit}b=1;next} f&&b' skills/ll-goal/references/goal-template.md | wc -c)" -ge 500 ]` exit 0, and make the smoke run assert the count (`bash scripts/smoke-test.sh --only goal-autonomo | grep -q 'smoke test OK — 4 checks'`) so an unknown or misspelled section name is red, not `0 checks`.
Gaps: G-2 SC-03 / T6 green only in the owner's worktree · amend M2 (or add a `stop:` to it) — the phase cannot prove `npm test` at HEAD while `hooks/ll-state.js` and `hooks/ll-precompact.js` stay uncommitted and I-09 forbids the executor from committing them; record it as an owner action with the resume condition, and prove the milestone on a clean tree: `d=$(mktemp -d) && git archive HEAD | tar -x -C "$d" && git -C "$d" init -q && git -C "$d" add -A && (cd "$d" && npm run lint && npm test)` exit 0.
Gaps: G-3 SC-02's "documented in SKILL.md" unchecked · amend M1 — `grep -q '^## Autonomous mode' skills/ll-goal/SKILL.md && [ "$(grep -c -- '--autonomous' skills/ll-goal/SKILL.md)" -ge 2 ]` exit 0.
Gaps: G-4 unsourced rule and unstated line budget · amend M1 — drop "never `/clear` between stages" or cite its source (it contradicts skills/ll-auto/SKILL.md:56), and state the line budget for the variant (`## Autonomous variant` + `## Autonomous example` ≤ 60 lines) so the 4,000-char and 150-line ceilings are reconcilable.
