# PLAN-REVIEW — phase 02 — 2026-09-10
mode: plan · one pass · plan: phases/02/PLAN.md · contract: ROADMAP.md "Phase 02" (SC-01..SC-03), PLAN.md §1 T4/T6, §2 I-01/I-08/I-09, §4 R-05, §7
verdict: REJECTED

| # | question | answer | milestone or line | severity |
|---|---|---|---|---|
| 1 | Does every milestone have an executable `acceptance:` (a command with an exit code)? | Partly. Four commands, all with exit codes; M1's leading `!` is a valid AND-list (`! false && echo` → exit 0, run here), so the lint defect is cosmetic as the brief says. Two acceptances do not test what the milestone claims: **(a)** M4 greps `ratified by absence` — but ROADMAP SC-02 requires both skills to record `[decided by absence — revisable]`; the acceptance asserts a string M4 itself invents (D-02-04) and would pass while SC-02 fails; contract > plan. **(b)** M1 adds two smoke checks and proves them with `bash -n` (a parser, not an execution); the two `contract_root` checks are first executed only at M4's `npm test`. The `(5 checked)` counter in M1's `behavior` is never asserted by the command, and `ls()`/`walk()` swallow ENOENT (`scripts/lint-contract.cjs:36-42`), so a mistyped `--root` yields `ok 6 next-targets (0 checked)` exit 0 and the good-fixture half of the acceptance passes vacuously. | PLAN.md:94 (M4), PLAN.md:63 (M1) | HIGH |
| 2 | Does every `truth:` in PLAN §1 have at least one milestone naming it? | Yes for the two in scope: T4 → M1, M2 (`truths:` lines 14, 25); T6 → M2, M3, M4 (lines 25, 36, 47). Residual: SC-03's own command (`npm run lint && npm test`, = T6) is run by M4 alone, and M3/M4 are the same wave (Waves table, PLAN.md:104); nothing re-runs it after M3 rewrites the three byte-identical `decision-policy.md` copies. M3's own `npm run lint` contains `lint-prompts` rule 6 (md5), so the risk is contained, not closed. | M3/M4, PLAN.md:104 | LOW |
| 3 | Does every `DEC-` cited in PLAN or DECISIONS appear in a milestone or in the freedoms? | Partly. D-02-01 → M1 action (line 61); D-02-03 → M3 (line 83); D-02-04 → M4 (line 93); `decisions/DEC-0001-auto-decision-flag.md` → M3 read_first (line 82). **D-02-02** appears by id nowhere (only obliquely, in the freedoms: "whether `--root` is a flag or an env var"); **D-02-05** appears by id nowhere — its content is buried inside M3's action prose. And `decisions/DEC-0007-smoke-test-owner-lift.md`, the only owner record that lifts I-09 on `scripts/smoke-test.sh`, is cited neither in PLAN nor in DECISIONS (see #8c). | M1 (D-02-02), M3 (D-02-05) | LOW |
| 4 | Is there scope reduction in the wording ("v1", "for now", "later", "simplified") without a deferred item? | No. A case-insensitive grep over `phases/02/PLAN.md` for `v1|for now|later|simplif|minimal|out of scope|good enough` returns only "the **minimal** frontmatter shape for the two fixture skills" (line 60), a shape reference, not a cut. The one real deferral — how `ll-auto --auto-decision` resolves the WAITING DECs `--no-talk` leaves — is recorded in `phases/02/DECISIONS.md` under `## Deferred` with a resume condition ("phase 03 plan"), and M3's action states the frozen-contract consequence explicitly. | — | NONE |
| 5 | Do `depends_on:` and `files:` agree — does a milestone read a file a later milestone creates? | No inversion: M2 `depends_on: [M1]` and reads `lint-contract.cjs (after M1)`; M3/M4 `depends_on: [M2]` and their file sets are disjoint, so wave 3 is safe with `exclusive: []`. But `files:` and the tree disagree in three places. **(a)** M2's action enumerates the rewrites and closes with "Do not change any other sentence", yet three handoffs in its own file list are never enumerated: `ll-close/SKILL.md:32` (`` `/clear`, then `ll-verify NN` ``), `ll-brainstorm/SKILL.md:127`, `ll-research/SKILL.md:82` — the last one is a bare handoff with trailing prose containing backticks, which the M1 grammar allows only inside a wrapping backtick pair, so it needs a parenthetical the plan never specifies. **(b)** M2 lists `assets/preamble.md`, which rule 6 never scans (`SKILL_TREE` only, `scripts/lint-contract.cjs:60`, `:311`): no acceptance in the phase verifies that edit. **(c)** `README.md:40` carries the same spec line in Portuguese (`▶ Next — /clear, depois <comando>`) and is in no milestone's `files:`. | M2, PLAN.md:69-71 | MED |
| 6 | Does any milestone touch more than 5 files or more than 3 tasks? | Yes — and beyond the known `files-too-many` on M2/M3. **M1 carries 4 tasks** in one milestone under `tdd: yes` (two commits): rewrite rule 6's grammar; add `--root` above line 60 so the module-load consts see it; author two fixture trees; add two smoke-test checks. The `test(M1)` commit owns tasks 3-4 and the `feat(M1)` commit tasks 1-2, so the TDD split does not reduce the milestone; the `--root` plumbing (a flag that rewrites `ROOT` before ten module-level consts, CODE-CONTEXT "Traps") is separable from the grammar and is the piece with no analog in the repo. | M1, PLAN.md:7-17 | MED |
| 7 | Is there a numeric value (limit, rate, timeout, seed, price) without a source? | No. The 200/150 ceilings (M3, M4) are `scripts/lint-prompts.sh:177,179` and every target file has headroom (`wc -l`: ll-decide 136, ll-close 69, interview 102, premise-gate 83, decision-policy 118, delivery 75). md5 `7cb5e151…` is verified in CODE-CONTEXT. `(5 checked)` in M1's behavior equals the 5 allowed forms M1 itself specifies for `next-good`; "two FAIL lines" equals the 2 bad lines specified for `next-bad`. "ten-minute silence" (M3) is the house rule already in `decision-policy.md`. Only weakness: the counters are self-referential to the fixtures the same milestone writes, and the acceptance discards stdout (#1b). | — | NONE |
| 8 | Is there a rule (an invariant or a prohibition in PLAN §2) without a source? | Yes — one of them blocks the phase. **(a) BLOCKER:** M1's exemption reads "Prose that merely mentions `▶ Next` without the em dash is ignored (ll-implement/SKILL.md:96, ll-resume/SKILL.md:76 today)". The second citation is false: `skills/ll-resume/SKILL.md:76` is `` last line `▶ Next — <cmd>` `` — em dash present (verified: `grep -c '▶ Next\s*—'` → 1; `hexdump` shows the marker inside backticks), and today's rule 6 already counts it (`ok 6 next-targets (23 checked)` = 24 markers under `skills/` minus the one genuinely em-dash-free line, ll-implement:96). Under the M1 grammar its tail, after the wrapping backtick pair is removed, is `<cmd>` — no `/clear, then` prefix — so it FAILs; and M2 classifies :76 as "prose about the line", does not list it, and forbids touching any other sentence. As written, M2's acceptance (`node scripts/lint-contract.cjs --rule 6`) and ROADMAP SC-01 ("passes on the tree") cannot be reached. **(b)** The grammar clause "or — only when the handoff was backtick-wrapped inside prose — by free prose after the closing backtick" (M1 action) is in neither D-02-01 ("no backticks around `/clear` or the command; one command per line") nor §4 R-05; it is the allowance the tree needs (ll-verify:72, ll-implement:88, ll-refine:111) but it has no recorded source. **(c)** §7 says "never commit the three files of I-09"; D-02-02 (band 2) waives it for `scripts/smoke-test.sh` by reading I-09 as phase-01-scoped, while the owner record that actually lifted it, `decisions/DEC-0007`, grants the edit only for "Phase 01 M5". The worktree confirms the file is clean at `122a3bb`, so the risk is authority, not collision. | M1 PLAN.md:61-62, M2 PLAN.md:71, D-02-02 | HIGH |

BLOCKS: report — M1's grammar exemption cites `skills/ll-resume/SKILL.md:76` as a line without the em dash; the file contradicts it (line 76 carries `▶ Next —` and is one of the 23 lines rule 6 checks today), so M2's acceptance and SC-01 "passes on the tree" are unreachable as planned (phases/02/PLAN.md:61,69,71).

Disconfirmation: 1 partial requirement — SC-01 is delivered only for the fixtures (M1) and for the 21 enumerated handoffs (M2); "passes on the tree" is not, and three in-scope handoffs (ll-close:32, ll-brainstorm:127, ll-research:82) are in `files:` but in no rewrite instruction. 1 acceptance that passes without testing — M4's `grep -q 'ratified by absence' skills/ll-close/SKILL.md` asserts a constant M4 writes in the same commit and which is not SC-02's contract string `[decided by absence — revisable]`; M1's `bash -n scripts/smoke-test.sh` parses the two new checks without running either. 1 uncovered error path — nothing in the phase covers rule 6's original failure mode ("Next names a skill that does not exist", `lint-contract.cjs:322-327`): `next-bad` holds an old-shape line and a bare line, both naming the fixture's own real skill, so a grammar rewrite that drops the resolve-the-target check is scored green (I-01: an existing check may not be weakened); and the sibling path, `--root` pointing at a missing directory (`ls()` returns `[]`, `walk()` returns `[]`, exit 0 "0 checked"), makes the `next-good` half of M1's acceptance pass vacuously.

What this review does NOT prove: that the rewritten rule 6 regex is expressible (no code was written or run against the 24 real handoffs — only today's rule 6 was executed, `ok 6 next-targets (23 checked)`); that M3's paragraphs fit the 150/200-line ceilings after drafting; that `npm test` is green in this worktree (not run — PROGRESS.md and hooks/* are dirty); that phase 02 may edit `scripts/smoke-test.sh` at all (an owner call, #8c).

Gaps: G-1 SC-01 cannot pass on the tree · amend M1 (exemption text) and M2 (rewrite list) so `skills/ll-resume/SKILL.md:76` is either rewritten to `` `▶ Next — /clear, then <cmd>` `` or exempted by a rule that matches it · acceptance: `node scripts/lint-contract.cjs --rule 6` exit 0
Gaps: G-2 SC-02's recorded string · amend M4 to write `[decided by absence — revisable]` (keeping "ratified by absence" only as prose) and to grep the contract string · acceptance: `grep -q '\[decided by absence — revisable\]' skills/ll-close/SKILL.md && grep -qE '^argument-hint:.*--no-talk' skills/ll-close/SKILL.md` exit 0
Gaps: G-3 three handoffs in `files:` and in no instruction · amend M2's action to name `ll-close/SKILL.md:32`, `ll-brainstorm/SKILL.md:127` and `ll-research/SKILL.md:82` (the last needs its trailing prose moved into a parenthetical) · acceptance: `node scripts/lint-contract.cjs --rule 6 && npm run lint` exit 0
Gaps: G-4 the fixtures do not cover rule 6's original check nor an empty `--root` · amend M1: add to `next-bad` a grammar-correct line naming a non-existent skill, and assert the counter · acceptance: `! node scripts/lint-contract.cjs --rule 6 --root scripts/fixtures/next-bad && node scripts/lint-contract.cjs --rule 6 --root scripts/fixtures/next-good | grep -q '(5 checked)'` exit 0
Gaps: G-5 the source that allows editing `scripts/smoke-test.sh` in phase 02 · amend M1 (or DECISIONS D-02-02) to cite an owner record whose scope covers this phase; `decisions/DEC-0007` covers only phase 01 M5 · acceptance: `grep -q 'phase 02' decisions/DEC-0007-smoke-test-owner-lift.md` exit 0 (or a new DEC cited in M1)
