# VERIFICATION — phase 01 — 2026-09-10
mode: plan · slice: main (working tree, PLAN not yet executed)
verdict: REJECTED · product: FAIL · process: OK

| # | question | answer | milestone or line | severity |
|---|---|---|---|---|
| 1 | Does every milestone have an executable `acceptance:`? | partly — all four are commands with exit codes, but (a) no milestone's acceptance runs `npm test`, so ROADMAP SC-04 (`npm run lint && npm test` exit 0) has no executable acceptance anywhere in the plan; ran `npm test` in this session at HEAD+worktree → exit 1, no output (set -e dies at `backlog-reconcile --cwd "$NEST/docs/state"` because the three lines added at scripts/smoke-test.sh:190-192 delete that dir first); (b) M4's acceptance never executes an assert: `run.sh --dry-run` only prints case blocks (scripts/evals/run.sh:312-314) and skips `assert.sh` (:225-233), so `bash -n` + `grep -q 'no_tool_use()'` is presence-only for the "assert the new behaviour" half of SC-05 | PLAN.md:60, 90; ROADMAP.md:18 | high |
| 2 | Does every `truth:` in PLAN §1 have a milestone naming it? | named yes, proved no — T1→M1, T2→M2, T6→M1..M4; but T6's own command is `npm run lint && npm test` (PLAN.md:28) and no acceptance runs `npm test`; M1 and M2 run `scripts/lint-prompts.sh` alone, never `npm run lint` (which is lint-prompts + `lint-contract.cjs`, package.json:scripts.lint). Four milestones claim T6; none executes it. T3/T4/T5 belong to phases 02–04, correctly absent | phases/01/PLAN.md:14,25,36,47 | high |
| 3 | Does every cited `DEC-` appear in a milestone or in the freedoms? | no — DEC-0002 appears (M2, line 68) and D-01-03 (M1, line 59); D-01-01, D-01-02, D-01-04, D-01-05 are enacted without their ids; D-01-06 (= PLAN §2 I-09: no executor touches `hooks/ll-state.js`, `hooks/ll-precompact.js`, `scripts/smoke-test.sh`) appears nowhere in the phase plan — no read_first, no milestone, no freedom, and M2/M3/M4 say only "Commit `feat(Mn): …`" with no file restriction (only M1 says "with only the 12 files"), so an executor committing with `-a` sweeps the owner's uncommitted work | DECISIONS.md D-01-06; phases/01/PLAN.md:68,78,89 | med |
| 4 | Scope reduction in the wording without a deferred item? | none found — the only `v1` is the preamble marker version (line 66, kept by D-01-03); M3's "em desenvolvimento" for `ll-auto` points at ROADMAP phase 03; the one deferral (Delegation/Decisions/Proof out of the preamble) is recorded in DECISIONS ## Deferred and ROADMAP:20 | phases/01/PLAN.md:66,78 | — |
| 5 | Do `depends_on:` and `files:` agree? | no — M1 adds to rule 3 a check that FAILs when `assets/preamble.md` contains `Route every request` or `One word from the owner`, and M1's acceptance runs the **whole** lint; the strings are still there at that point (assets/preamble.md:4 and :21, verified in this session) and only M2 removes them (wave 2, `depends_on: [M1]`), so M1's acceptance is unsatisfiable at M1. Secondary: M4 (`depends_on: []`, wave 1) writes asserts for behaviour that only M2's preamble produces (`no_tool_use Skill`, the session naming `/ll-research`); only `--dry-run` hides it. M2→M1 and M3→M2 orders are correct; all 12 M1 files and all 9 M4 files exist in HEAD | phases/01/PLAN.md:12,43,59,98 | high |
| 6 | Any milestone over 5 files or over 3 tasks? | file counts are the two accepted lint warns (not repeated). Tasks: M1 is one opus milestone carrying six distinct edits to `scripts/lint-prompts.sh` (rule1 flag membership, rule1 length bound, rule1 "Use when" inversion, rule3 ceiling, rule3 new string check, header comment) plus 11 description rewrites plus 8 frontmatter additions — two milestones' worth under one acceptance, and the rule3 string check is the piece that breaks it (see #5) | phases/01/PLAN.md:59 | low |
| 7 | A numeric value without a source? | none found — 60–300 [ROADMAP.md:17 / ASM-1], ceiling 70 [scripts/lint-prompts.sh:185-186, D-01-03], `3.0.0` [PLAN.md:20, D-01-05 keeps package.json for phase 05], `/ll-implement 0?7` traces to the fixture prompt "implementa a fase 7" [scripts/evals/cases/router-execute/prompt.txt, verified], 12/8/11 file counts [CODE-CONTEXT, verified] | phases/01/PLAN.md:59,68,79,89 | — |
| 8 | A rule or prohibition without a source? | one — "no backticked skill names" in descriptions (M1 action) is in neither ROADMAP SC-03 nor ASM-1 nor any lint rule: rule1 checks length, first word, "Use when", argument-hint only (scripts/lint-prompts.sh:119-150), and rule5's `` run `ll-x` `` ban scans the body from `body_start` (scripts/lint-prompts.sh:268-273), never the frontmatter. Invented out of caution; "no trigger phrases" is sourced (ASM-1) | phases/01/PLAN.md:59 | low |

BLOCKS: none (nothing executed yet; process checks do not apply to an unexecuted plan)

Confrontation: PROGRESS not opened — plan mode, no milestone claims to compare.

Disconfirmation: 1 partial requirement — SC-05 is half-covered: the `--dry-run --all` half has a command, the "assert the new behaviour" half has only `bash -n` and a `grep` for a function name. 1 check that passes without checking — M4's `grep -q 'no_tool_use()' scripts/evals/lib/assert.sh` goes green on an empty function body; the helper's scan over `tool_use` blocks is never executed by anything in the plan. 1 uncovered error path — no milestone states what happens when `npm test` is red for a reason inside the three I-09 files: the plan has no branch for it, and it is red right now.

What this verification does NOT prove: that the four milestones, once executed, satisfy SC-01/SC-02/SC-03 — no code was written or run against them; that the rewritten eval cases pass a real `claude` rep (SC-05's behaviour half needs `bash scripts/evals/run.sh --case router-research --reps 1`, which the plan never runs); that `npm test` can be made green without touching `scripts/smoke-test.sh`.

Deferred: moving Delegation/Decisions/Proof out of the preamble — until the owner asks, after phase 03 (DECISIONS ## Deferred).

Gaps: G-1 M1 — the new rule-3 check for `Route every request` / `One word from the owner` cannot land in M1 while the preamble still carries both (assets/preamble.md:4,21): move that check into M2 (add `scripts/lint-prompts.sh` to M2's files) or narrow M1's acceptance · acceptance: `bash scripts/lint-prompts.sh && test "$(grep -L 'disable-model-invocation: true' skills/*/SKILL.md | wc -l)" = 0` exit 0 at the end of the milestone that owns the check
Gaps: G-2 new milestone (or amend M3) covering SC-04 and T6 · owner: `npm test` exits 1 today at scripts/smoke-test.sh:190-192 (owner's uncommitted lines delete `$NEST/docs/state` before `backlog-reconcile` reads it) and I-09/D-01-06 forbid any executor touching that file — the owner decides: fix it themselves, lift I-09 for those three lines, or defer SC-04 to phase 02 · acceptance: `npm run lint && npm test` exit 0
Gaps: G-3 M4 — the acceptance must execute at least one assert, not only lint its syntax · acceptance: `bash scripts/evals/cases/router-research/assert.sh <workdir> <recorded out.json> <out.txt>` exit 0 against a checked-in capture fixture
Gaps: G-4 M2, M3, M4 — the commit line must name its files, as M1's does, so I-09/D-01-06 survives an executor · acceptance: `git show --stat HEAD` lists only the milestone's `files:` and none of `hooks/ll-state.js`, `hooks/ll-precompact.js`, `scripts/smoke-test.sh`
Gaps: G-5 M1 — drop or source the "no backticked skill names" prohibition (band-3 rule invented out of caution) · acceptance: the phase plan cites ROADMAP SC-03 or ASM-1 for every constraint it puts on a description
